# Local Dev Tailscale DNS Configuration

# Domain pattern (regex) - matches domains to include in DNS
# Default: \.dev (matches all .dev domains)
# Examples:
#   DOMAIN_PATTERN="\.dev"                    # All .dev domains
#   DOMAIN_PATTERN="\.(dev|local)"            # .dev and .local domains
#   DOMAIN_PATTERN="\.local\.dev"             # Only .local.dev domains
#   DOMAIN_PATTERN="^(app|api)\..*\.dev"      # Specific subdomains
DOMAIN_PATTERN="\.dev"

# Host IP pattern (regex) - matches source IPs in /etc/hosts
# Default: ^(127\.0\.0\.1|10\.0\.0\.1) (matches localhost and local proxy)
# Examples:
#   HOST_IP_PATTERN="^127\.0\.0\.1"           # Only localhost
#   HOST_IP_PATTERN="^(127\.0\.0\.1|10\..*)"  # Localhost and 10.x.x.x
#   HOST_IP_PATTERN="^192\.168\."             # Local network range
HOST_IP_PATTERN="^(127\.0\.0\.1|10\.0\.0\.1)"

# Certificate configuration for HTTPS support
# Certificate wildcard domain for mkcert (used for HTTPS)
# This should match your domain pattern (e.g., "*.dev", "*.local.dev")
# Multiple domains can be specified, space-separated
# Default: *.dev localhost 127.0.0.1
CERT_DOMAINS="*.dev localhost 127.0.0.1"

# Certificate export directory (for sharing with other devices)
# Default: ./certs (relative to script location)
CERT_EXPORT_DIR="./certs"
